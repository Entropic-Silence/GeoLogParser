# Annotation schema

The machine-output contract currently points to
`../../schemas/borehole_v001.schema.json`. Annotation workflow extensions
(annotator, round, status, adjudication, and agreement) are `NOT COMPLETED` and
will be versioned separately from extraction output.

