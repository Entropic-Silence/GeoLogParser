Read only the populated geology/depth log on the page. Return every visible lithologic interval in source units as ordered `top_depth_source`, `bottom_depth_source`, and `lithology_raw`. Do not use values from casing, annular material, water level, total depth, or location fields. Preserve the page's interval boundaries and do not repair, infer, merge, split, reorder, or deduplicate records. If no populated geology log is visible, return `{"intervals": []}`.
