# Annotation/review application

`NOT COMPLETED` in the foundation round. The planned UI shows the page on the
left and provenance-bearing fields on the right, supports bbox highlighting,
field editing, validation, field re-reading, review queues, and JSON/CSV/XLSX
export. Implementation begins only after the v001 schema is exercised on real,
rights-cleared samples.

